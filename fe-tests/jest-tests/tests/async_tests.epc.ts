// test("parse invalid Lcov report", async () => {
//           const lcovFilePath = path.join(resPath, "lcov_inv.info");
//           //const coverages = await parseLcov(lcovFilePath);
//           // await expect(parseLcov(lcovFilePath))
//           //     .rejects
//           //     .toThrow("Parsing error occured when processing lcov content");
//           await expect(parseLcov(lcovFilePath)).rejects.toEqual("Parsing error occured when processing lcov content");
//           expect(true).toBe(true);
//       });
  